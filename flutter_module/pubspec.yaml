name: flutter_module
description: A new flutter module project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  pigeon:

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: co.leancode.flutter_module
    iosBundleIdentifier: co.leancode.flutterModule
